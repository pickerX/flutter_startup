import 'dart:convert';

import 'package:flutter_startup/pages/crypto/crypto.page.dart';

class SHAPage extends CryptoPage {
  SHAPage({super.key}) : super(title: "SHA加密/解密");

  @override
  String encode(String? input) {
    String result = '';

    return result;
  }

  @override
  String decode(String? input) {
    String result = '';

    return result;
  }
}
